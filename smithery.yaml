runtime: "container"
startCommand:
  type: "stdio"
  command: "node"
  args: ["index.js"]
description: "Tamper-evident audit trail for AI agents. Every action hashed into an immutable chain. Prove what happened — to your boss, auditor, or courtroom."
